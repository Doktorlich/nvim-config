vim.diagnostic.config({
  virtual_text = true, -- текст в конце строки (как у вас сейчас)
  float = {
    border = 'rounded',
    source = 'always',
    header = '',
    prefix = '',
    wrap = true, -- включить перенос строк в окне
  },
})

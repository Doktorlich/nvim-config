-- Автозакрытие и синхронное переименование JSX/HTML-тегов
-- (<div> -> сам допишет </div>, переименовали открывающий тег -> закрывающий меняется сам)
return {
  "windwp/nvim-ts-autotag",
  event = "InsertEnter",
  opts = {},
}
